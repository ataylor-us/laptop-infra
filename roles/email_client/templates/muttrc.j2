# {{ ansible_managed }}
# vim: filetype=neomuttrc
# This config is a modified version of the ones that ship with mutt-wizard (https://github.com/LukeSmithxyz/mutt-wizard)
set send_charset="us-ascii:utf-8"
set mime_type_query_command = "file --mime-type -b %s"
set date_format="%Y-%m-%d %H:%M" 
set index_format="%2C %Z %?X?A& ? %D %-15.15F %s (%-4.4c)"
set sort = 'reverse-date'
set rfc2047_parameters = yes
set sleep_time = 0		# Pause 0 seconds for informational messages
set markers = no		# Disables the `+` displayed at line wraps
set mark_old = no		# Unread mail stay unread until read
set mime_forward = no	# mail body is forwarded as text
set forward_attachments = yes	# attachments are forwarded with mail
set wait_key = no		# mutt won't ask "press key to continue"
set fast_reply			# skip to compose when replying
set fcc_attach			# save attachments with the body
set forward_format = "Fwd: %s"	# format of subject when forwarding
set forward_quote		# include message in forwards
set reverse_name		# reply as whomever it was to
set include			# include message in replies
set mail_check=60 # to avoid lags using IMAP with some email providers (yahoo for example)
set mailcap_path = $HOME/.config/mutt/mailcap:$mailcap_path
auto_view text/html		# automatically show html (mailcap uses lynx)
#set display_filter = "tac | sed '/\\\[-- Autoview/,+1d' | tac" # Suppress autoview messages.
alternative_order text/plain text/enriched text/html
# Unbinds
bind index,pager i noop
bind index,pager g noop
bind index \Cf noop
bind index,pager M noop
bind index,pager C noop
bind editor <space> noop
bind index h noop

# Refreshing mail
bind index o imap-fetch-mail
bind index O imap-fetch-mail

# Replace urlview with urlscan
macro index,pager \cb "<pipe-message> urlscan<Enter>" "call urlscan to extract URLs out of a message"
macro attach,compose \cb "<pipe-entry> urlscan<Enter>" "call urlscan to extract URLs out of a message"

# General rebindings
bind index gg first-entry
bind index j next-entry
bind index k previous-entry
bind attach <return> view-mailcap
bind attach l view-mailcap
bind index G last-entry
bind pager,attach h exit
bind pager j next-line
bind pager k previous-line
bind pager l view-attachments
bind index L limit
bind index l display-message
bind index,query <space> tag-entry
#bind browser h goto-parent
macro browser h '<change-dir><kill-line>..<enter>' "Go to parent folder"
bind index,pager H view-raw-message
bind browser l select-entry
bind browser gg top-page
bind browser G bottom-page
bind pager gg top
bind pager G bottom
bind index,pager,browser d half-down
bind index,pager,browser u half-up
bind index,pager S sync-mailbox
bind index,pager R group-reply
bind index \031 previous-undeleted	# Mouse wheel
bind index \005 next-undeleted		# Mouse wheel
bind pager \031 previous-line		# Mouse wheel
bind pager \005 next-line		# Mouse wheel
bind editor <Tab> complete-query
macro index \Cr "T~U<enter><tag-prefix><clear-flag>N<untag-pattern>.<enter>" "mark all messages as read"
macro index A "<limit>all\n" "show all messages (undo limit)"
set sidebar_visible = yes
set sidebar_width = 14
set sidebar_short_path = yes
set sidebar_next_new_wrap = yes
set mail_check_stats
set sidebar_format = '%D%?F? [%F]?%* %?N?%N/? %?S?%S?'
bind index,pager \Ck sidebar-prev
bind index,pager \Cj sidebar-next
bind index,pager \Co sidebar-open
bind index,pager \Cp sidebar-prev-new
bind index,pager \Cn sidebar-next-new
bind index,pager B sidebar-toggle-visible
# Uses khard to add emails to your contacts
set query_command = "khard email --parsable '%s'"
# Uses khard to look up emails in your contacts
macro pager,index a "<pipe-message>khard add-email<return>" "add the sender address to khard"
set editor="nvim -u $HOME/.config/mutt/vimrc"
set signature="~/.config/mutt/signature"
set realname = "{{ full_name }}"
set from = "{{ personal_email }}"
set sendmail = "msmtp -a {{ personal_email }}"
alias me ataylor <{{ personal_email }}>
set folder = "imaps://{{ personal_email }}@{{ personal_email_imap_domain }}:993"
set header_cache = "~/.cache/mutt"
set message_cachedir = "~/.cache/mutt"
set mbox_type = Maildir
set hostname = "{{ personal_email_hostname }}"
set spoolfile = "+INBOX"
set postponed = "+Drafts"
set trash = "+Trash"
set record = "+Sent"
set imap_user = "{{ personal_email }}"
set imap_pass = "`{{ personal_mail_pass_command }}`"
set ssl_starttls = yes
set ssl_force_tls = yes
{% if ansible_facts['os_family'] == 'Darwin' %}
startup-hook 'stty discard undef'
source ~/.config/mutt/theme.neomuttrc
{% endif %}
mailboxes "=INBOX" "=Drafts" "=Sent" "=Archive" "=Spam" "=Junk" "=Trash"
macro index,pager D ";<save-message>=Trash<enter>" "move mail to trash"
macro index,pager A ";<save-message>=Archive<enter>" "archive mail"
macro index,pager I ";<save-message>=INBOX<enter>" "move mail to inbox"
set status_format = "[🏡Home🏡] -%r-NeoMutt: %D [Msgs:%?M?%M/?%m%?n? New:%n?%?o? Old:%o?%?d? Del:%d?%?F? Flag:%F?%?t? Tag:%t?%?p? Post:%p?%?b? Inc:%b?%?l? %l?]---(%?T?%T/?%s/%S)-%>-(%P)---"
