---
- name: Install formulae
  ansible.builtin.import_tasks: common/tasks/install_brew_formulae.yml
  vars:
    brew_formulae:
      - mas
- name: Install apps using mas
  become: true
  community.general.mas:
    id: "{{ item }}"
    state: present
  loop: "{{ mas_apps }}"
